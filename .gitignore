node_modules
.next
out
.DS_Store
*.tsbuildinfo
next-env.d.ts

# prototype-only: the symlink in the repository root that lets the static
# preview resolve /shots. Anchored, or it would also swallow public/shots.
/shots
